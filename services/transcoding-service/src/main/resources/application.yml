spring:
  application:
    name: transcoding

kafka:
  bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  endpoint:
    health:
      show-details: always

minio:
  endpoint: http://localhost:9100
  access-key: minio
  secret-key: minio123

transcoding:
  concurrency: 4
